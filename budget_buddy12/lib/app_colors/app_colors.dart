import 'package:flutter/material.dart';

class AppColors {
  // Gradient for cards and splash
  static const gradientStart = Color(0xFFa8edea);
  static const gradientEnd = Color(0xFFfed6e3);

  // Bottom navigation bar
  static final bottomNavSelected = Colors.pinkAccent;
  static const bottomNavUnselected = Colors.black54;

  // Transaction text colors
  static const incomeText = Colors.green;
  static const expenseText = Colors.red;
  static const balanceText = Colors.blueAccent;

  // Pie chart colors
  static const incomeCircle = Colors.green;
  static const expenseCircle = Colors.red;

  // Action colors
  static const deleteIcon = Colors.red;
  static const fabBackground = Colors.black54;

  // Card shadows
  static const cardShadow = Colors.black12;
}
